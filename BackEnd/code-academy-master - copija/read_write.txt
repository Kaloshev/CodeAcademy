const fs = require("fs")

let rawdata = fs.readFileSync("student.json");
let student = JSON.parse(rawdata);
// console.log(student);

fs.readFile("student.json", (err, data) => {
    if (err) throw err;
    let student = JSON.parse(data);
    // console.log(student)
})

// console.log("This is after the read call")

let student2 = {
    name: 'Mike',
    age: 23,
    gender: 'Male',
    department: 'English',
    car: 'Honda'
};

let data = JSON.stringify(student2, null, 2);
fs.writeFileSync("student2.json", data)

let student3 = {
    name: 'Mike',
    age: 23,
    gender: 'Male',
    department: 'English',
    car: 'Honda'
};

let data1 = JSON.stringify(student3, null, 2);

fs.writeFile("student3.json", data1, (err) => {
    if (err) throw err;
    console.log("Data written to file")
    // console.log(data1)
})

console.log("This is after the write call")

